import Styled from '@common/StyledComponents';

const ChangePassword = () => {
  return (
    <Styled.View>
      <Styled.Text>ChangePassword</Styled.Text>
    </Styled.View>
  );
};

export default ChangePassword;
