import Styled from '@common/StyledComponents';

const Settings = () => {
  return (
    <Styled.View>
      <Styled.Text>Settings</Styled.Text>
    </Styled.View>
  );
};

export default Settings;
