import Styled from '@common/StyledComponents';

const Support = () => {
  return (
    <Styled.View>
      <Styled.Text>Support</Styled.Text>
    </Styled.View>
  );
};

export default Support;
